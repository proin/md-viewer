# Markdown Viewer 개발

이 폴더의 소스를 수정하여 앱을 업데이트합니다.

- `src/`는 화면·편집기·미리보기, `electron/`은 파일 처리와 macOS 기능입니다.
- 의존성을 변경하면 `package.json`과 `package-lock.json`을 함께 갱신합니다.
- 변경 후 `npm test`와 필요한 앱 검사를 실행합니다. `npm run test:e2e`는 먼저 빌드한 뒤 모든 앱 검사를 실행합니다.
- 앱 검사는 임시 문서와 별도 사용자 설정 폴더를 사용합니다. 실제 사용자의 문서를 검사 자료로 수정하지 않습니다.
- 화면 빌드 결과는 `dist/`, 배포용 앱은 `release/mac-arm64/Markdown Viewer.app`, 검사 화면 이미지는 `artifacts/screenshots/`에 저장합니다.
- 새 버전을 만들 때 `CHANGELOG.md`와 사용 방법을 함께 갱신합니다.
- 화면과 문서는 명확하고 익숙한 한국어로 작성합니다. 불필요한 전문용어와 모호한 표현, 구어체를 사용하지 않습니다.
- 문서 작업을 마치면 용어와 문장을 검토하고, 완료 응답에 검사 목록을 포함합니다.
